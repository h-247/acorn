# `packages/config`

Shared tooling configuration may live here after the implementation stack is finalized (TypeScript, linting, formatting, testing). Do not add framework-specific config before the framework decision is recorded.
