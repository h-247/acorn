# ADR-0005 — Teacher-in-the-Loop

**Status:** Accepted

Recommendation is advisory. AI output is a candidate. Teachers retain the final academic decision for what is approved/assigned.

No official workflow may silently perform:

```text
AI output → publish/assign to learner
```

without the required review/decision state.
