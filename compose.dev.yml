services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-acorn}
      POSTGRES_USER: ${POSTGRES_USER:-acorn}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-acorn_dev_only}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-acorn} -d ${POSTGRES_DB:-acorn}"]
      interval: 5s
      timeout: 5s
      retries: 10

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${OBJECT_STORAGE_ACCESS_KEY:-acorn}
      MINIO_ROOT_PASSWORD: ${OBJECT_STORAGE_SECRET_KEY:-acorn_dev_only}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data

volumes:
  postgres_data:
  minio_data:
